<Window x:Class="DaHua.ADRMS.SaveUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DaHua.ADRMS"
        mc:Ignorable="d"
         Style="{StaticResource SmallLayoutStyle}"
        Title="保存方案" Height="185" Width="400"  WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="38"></RowDefinition>
             <RowDefinition Height="50"></RowDefinition>
             <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" VerticalAlignment="Center" Orientation="Horizontal">
            <TextBlock Width="80" HorizontalAlignment="Center" Margin="50,5,0,0">方案名称：</TextBlock>
            <TextBox Width="200" x:Name="FCaseName" Style="{StaticResource SelectboxStyle}"></TextBox>
        </StackPanel>
        <Button Grid.Row="1" x:Name="BtnSave" Style="{StaticResource ButtonStyle}" Width="80" Height="30">保存方案</Button>
    </Grid>
</Window>
